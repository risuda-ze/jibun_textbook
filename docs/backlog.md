# あとでやること

## Phase 2（brainで決定済み）

- レッスン内の「AIに頼む」（定型ボタン＋自由入力1行。会話履歴なし。結果は常にAIの下書きブロックとして本文に入る）
- OpenAI互換API / ローカルモデル（PCのみ）。`src/ai/index.ts` にアダプタを足す。画面の切り替えは実装済みで「まだ使えない」と出る
- Markdown書き出し（brain vault 向け）

## 作りながら気づいたこと

- JSが約574KB（gzip 約172KB）。SDK・marked・turndown・DOMPurify が大きい。AI層を動的importにすれば初回表示が軽くなる
- 節の並べ替え（ドラッグ）と、章をまたぐ移動。今は追加・改名・削除だけ
- ノート入力欄の下書きは、差し込み位置を変えると消える。位置を変えても残す
- 画像に説明文（alt）を付ける欄
- 生成の途中でやめるボタン
- `claude-opus-5` / `claude-fable-5-1` のサーバー側フォールバック（応答を控えられたときの自動切り替え。ベータ）
- 書き出し忘れの知らせを、本棚だけでなくレッスン画面にも出すか
